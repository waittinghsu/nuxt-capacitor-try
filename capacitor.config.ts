import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'example.omega.com',
  appName: 'nuxt-app',
  webDir:  'dist', // Nuxt 構建生成目錄
};

export default config;
